# =============================================================================
# Indra — development tooling
#
# Install on top of the runtime set:
#     pip install -r requirements.txt -r requirements-dev.txt
#
# Nothing here is imported by src/indra at runtime, and none of it is installed
# into the production container image (see docker/Dockerfile.backend).
# =============================================================================

# -----------------------------------------------------------------------------
# Linting and formatting
#
# ruff covers the pyflakes/pycodestyle/isort surface in one pass; black owns
# formatting so style is never a review discussion.
# -----------------------------------------------------------------------------
ruff==0.6.9
black==24.8.0

# -----------------------------------------------------------------------------
# Static type checking
#
# The geospatial and array libraries ship incomplete or absent stubs, so mypy
# is configured to check src/indra strictly while ignoring missing imports
# from third-party GIS packages (see pyproject/mypy config when added).
# -----------------------------------------------------------------------------
mypy==1.11.2
types-PyYAML==6.0.12.20240808
types-requests==2.32.0.20240914

# -----------------------------------------------------------------------------
# Test framework
#
# Present so the repository carries a conventional test entry point. Any tests
# added here must exercise pure logic — coordinate transforms, tensor shape
# contracts, schema validation — against inputs constructed in the test itself.
# They must never fabricate synthetic geophysical fields to stand in for
# INSAT/IMDAA/IMD observations.
# -----------------------------------------------------------------------------
pytest==8.3.3
pytest-cov==5.0.0
